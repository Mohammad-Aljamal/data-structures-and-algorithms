"use strict";

const tree = require('tree');
const Node = require('../Node');


class BinaryTree  {
  constructor(root) {
    this.root = root;
  }
}


breadthFirst (tree) {
    if(!tree.root) {
        return 'the tree is empty';
    }

    const arr = [ tree.root ];
    const curr = 
    // if(tree.r){

    // }
}



module.exports = BinaryTree;
